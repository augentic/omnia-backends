name: Checkout omnia
description: Check out the local omnia dependency used by [patch.crates-io] (requires omnia-backends checkout first)

runs:
  using: composite
  steps:
    - uses: actions/checkout@v6
      with:
        repository: augentic/omnia
        ref: refactor
        path: ../omnia
