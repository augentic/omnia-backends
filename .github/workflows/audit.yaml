name: Audit

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  checks: write

jobs:
  audit:
    uses: augentic/.github/.github/workflows/audit.yaml@main
    secrets: inherit
